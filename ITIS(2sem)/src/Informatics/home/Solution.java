package Informatics.home;

import java.util.Arrays;
import java.util.List;
import java.util.Map;

/**
 * Created by User on 29.03.2017.
 */
public class Solution implements MapOperations {

    /*public static void main(String[] args) {
        List list = Arrays.asList("3", "4", "12", "67", "605", "1", "4");
        int a = (int) list.stream().filter("4" :: equals).count();
        String  b = (String) list.stream().findFirst().orElse(0);
        String c = (String) list.stream().skip(list.size() - 1).findAny().orElse("empty");
        System.out.println(list.toString());
        System.out.println(a + " " + b + " " + c);
    }*/

    @Override
    public Map<String, Integer> words(List<String> lst) {
        return null;
    }

    @Override
    public Map<String, Integer> transform(Map<Integer, String> map) {
        return null;
    }
}
